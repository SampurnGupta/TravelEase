#Thanks for contributing!

- Fork the repo and create a feature branch: `git checkout -b feat/your-feature`
- Keep changes focused and add tests where appropriate.
- Follow existing code style (Android Studio auto-format).
- Open a pull request against `main` with a short description of changes.